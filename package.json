{
  "name": "collage-splitter",
  "version": "5.0.0",
  "description": "Contact Sheet — Collage Splitter: split any image collage into individual frames, with advanced auto-detection, metadata scrubbing, and GitHub Pages support.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
